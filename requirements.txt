# Dependencias reais do projeto (pyproject.toml e a fonte da verdade).
# O pacote ats-scrapers traz pandas/numpy/httpx transitivamente.
# P0 deadline: ancoragem explicita no commit upstream oficial ae0ad53
# (contem o merge do PR #268); trocar pelo pin PyPI assim que publicado.
ats-scrapers @ git+https://github.com/kalil0321/ats-scrapers@ae0ad53
requests>=2.31
pydantic>=2.0
# Scraper avature (Siemens Healthineers, DHL) exige beautifulsoup4 (extra
# opcional do ats-scrapers); sem ele o fetch falha com ScraperError.
beautifulsoup4>=4.12
