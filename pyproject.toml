[project]
name = "internship-finder"
version = "0.1.0"
description = "International internship search engine"
readme = "README.md"
requires-python = ">=3.13"

dependencies = []

[project.scripts]
internship-finder = "internship_finder.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/internship_finder"]